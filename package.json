{
  "name": "goodebag-games",
  "version": "1.0.0",
  "description": "Goodebag Games official website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:wallet": "esbuild wallet-src/wallet.js --bundle --format=iife --global-name=WalletModule --outfile=public/js/wallet.bundle.js"
  },
  "dependencies": {
    "@walletconnect/modal": "^2.7.0",
    "express": "^4.18.2",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
